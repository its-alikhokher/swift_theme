Drop a variable Inter font here as `inter-var.woff2` for fully offline usage.
Download: https://github.com/rsms/inter/releases (Inter.var.woff2 -> rename to inter-var.woff2)
If missing, Swift Theme falls back to Google Fonts CDN automatically.
